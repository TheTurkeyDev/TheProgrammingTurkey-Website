import { Body1 } from 'gobble-lib-react';
import { ProjectWrapper } from '../../../../components/project-wrapper';

export const SportStreamWidgets = () => {

    return (
        <ProjectWrapper
            title='Sport Stream Widgets'
            subTittle=''
            links={[]}
            videos={[]}
        >
            <Body1>
                Provides widgets for sports streams to use to display various user & automatically controlled informational graphics.
            </Body1>
        </ProjectWrapper>
    );
};